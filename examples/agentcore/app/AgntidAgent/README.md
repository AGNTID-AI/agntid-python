# AgntID AgentCore application

This directory contains the Python application packaged by the AgentCore CLI.
Run commands from the `examples/agentcore` project root; the complete setup and
security notes are in that directory's README.
